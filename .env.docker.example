POSTGRES_DB=padel
POSTGRES_USER=padel
POSTGRES_PASSWORD=change_me_before_deploying
POSTGRES_PORT=5432
API_PORT=8080
WEB_PORT=4321

# Use this value for the Go API when it runs on the same host as Docker.
DATABASE_URL=postgresql://padel:change_me_before_deploying@localhost:5432/padel?sslmode=disable
